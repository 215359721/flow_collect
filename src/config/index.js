const baseUrl = 'http://192.168.1.202:2025/aspsemantic'
const useMockData = 1 //是否使用本地mock数据
const useExColor = 0 //泳道UI是否使用交替行
const isNewUI = 1 //是否使用新节点UI
export {
  baseUrl,
  useMockData,
  useExColor,
  isNewUI,
}